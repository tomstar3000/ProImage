<?
if (!defined('Allow Scripts')) {
	echo "Hacking Attempt";
	die();
}
/*
session_start();
if(isset($_SESSION['AdminLog'])){
	$loginsession = $_SESSION['AdminLog'];
	if($loginsession[0] == 0 || $loginsession[0] == false){
		$GoTo = "index.php";
		header(sprintf("Location: %s", $GoTo));
	}
} else {
	$GoTo = "index.php";
	header(sprintf("Location: %s", $GoTo));
}
*/
?>
